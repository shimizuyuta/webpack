// const hello = 'hello'

// export default class World {
//   message: string

//   constructor(message: string) {
//     this.message = message
//   }
// }
import { primitiveSample } from 'basic'
import { logMessage, sumProductsPrice } from 'function/basic'
primitiveSample()
logMessage('hello world', 'name')
const sum = sumProductsPrice(2, 3, 4, 5)
